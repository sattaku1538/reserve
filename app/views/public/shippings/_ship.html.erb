<% if shippins.present? %>
  <table class="table table-hover table-bordered">
    <thead>
        <tr class="info">
            <th>郵便番号</th>
            <th>住所</th>
            <th>宛名</th>
            <th colspan="2"></th>
        </tr>
    </thead>

    <tbody>
      <% shipping.each do |shipping| %>
        <tr class="shipping-tr" id="destroy-<%= shippings.id %>">
            <td>
                <%= shipping.post_code %>
            </td>
            <td class="text-left">
                <%= shipping.address %>
            </td>
            <td class="text-left">
                <%= shipping.name %>
            </td>
            <td>
                <%= link_to "編集する",edit_public_shipping_path(shippings), class: "btn-primary" %>
            </td>
            <td>
                <%= link_to "削除する",public_shippings_path(shippings), method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, remote: true, class: "btn-danger" %>
            </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>