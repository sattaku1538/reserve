<div class="container mt-3">
  <div class="row">
    <div class="col-md-9 mx-auto">
      <!--ジャンル検索窓のテンプレート-->
      <%= render 'public/products/category', categories: @categories %>
      <h2 class="mt-3"><strong>商品一覧</strong><small> (全<%= @products.count %>件)</small></h2>
        <%= render 'index', products: @products %>
        <%= paginate @products %>
      </div>
    </div>
  </div>
</div>