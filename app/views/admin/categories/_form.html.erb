<%= form_with(model: [:admin, category], local: true) do |f| %>
    <div class="form-inline my-5">
      <%= f.label :name, 'ジャンル名' %>
      <%= f.text_field :name, placeholder: 'ジャンル名', class:"form-control mx-4" %>
      <%= f.submit submit, class:"btn btn-success" %>
    </div>
<% end %>